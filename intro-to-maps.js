const person = {
    firstName: "James",
    age: 2
};
console.log(person);


// key & value can be anything, e.g. {}
// 後蓋前
const map = new Map();
map.set('firstName',"James")
map.set('age',2)
map.set('age',3)
console.log(map)